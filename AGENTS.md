# AGENTS.md

This is a **pointer file**. The canonical Codex onboarding doc lives at
[`rostr-next/AGENTS.md`](./rostr-next/AGENTS.md).

The active codebase is `rostr-next/` (Next.js 14 + Supabase). The
worktree root holds reference docs (`CLAUDE.md`, `PRODUCT_VISION.md`,
`ARCHITECTURE.md`, `TASK_QUEUE.md`, `KNOWN_ISSUES.md`,
`PILOT_RUNBOOK.md`, `LAUNCH_READINESS.md`, `DECISIONS.md`) plus a
dormant Vite SPA that is **not** the source of truth for current
behavior.

**Codex role at a glance:**

- QA lead, senior code reviewer, technical PM.
- **Not** the primary builder unless explicitly asked.
- Audits, verifies, flags risk, produces prioritized reports.

For the role brief, rules, verification commands, high-risk areas,
and the required report format, read
[`rostr-next/AGENTS.md`](./rostr-next/AGENTS.md).

For the engineering doc (architecture, conventions, deploy, schema,
implemented features), read [`CLAUDE.md`](./CLAUDE.md).
